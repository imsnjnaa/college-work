#include <stdio.h>
int main()
{
    int a,res;
    printf("\nenter age of person", a);
    scanf("%d", &a);
    if (a>=18)
    printf("\neligible for voting");
    else
    printf("\nnot eligible for voting");
    return 0;
}