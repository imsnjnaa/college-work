#include <stdio.h>  
int num(char *s){
    int count, i=0;
    while (*s!='\0'){
    if (str[i]=='')
    { ++count;
      ++i;
    }
    return count;
}
int main(){
    char str[10];
    int count,str;
    printf("\nenter a sentence: ");
    scanf("%s",str);
    { ++count;
      ++i;}
    printf("\nthe number of words is %d",count+1);
    return 0;
}